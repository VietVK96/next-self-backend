<head>
  <style>

    @page { margin: 15mm 5mm 5mm; padding: 0; } body { font-family: Arial;
    font-size: 12px; } #header { position: fixed; left: 0; right: 0; color:
    #aaaaaa; } #header { top: -10mm; } table { width: 100%; border-collapse:
    collapse; } table th, table td { padding: 2mm; border: 0.1pt solid #000000;
    white-space: nowrap; } table th.columnUser, table td.columnUser { width: 6%;
    text-align: center; } table th.columnDate, table td.columnDate { width: 20%;
    text-align: center; } table th.columnPatient, table td.columnPatient {
    width: 38%; } table th.columnName, table td.columnName { width: 36%; }

  </style>
</head>
<body>
  {{!-- <div id="header">
        {{#if (view === 'day') }}
        <strong>Planning du {{param?.start}}</strong>
        {{ else }}
        <strong>Planning du du</strong>
        {{/if}}
    </div> --}}
  <table>
    <thead>
      <tr>
        <th class='columnUser'>P</th>
        <th class='columnDate'>Date</th>
        <th class='columnPatient'>Patient</th>
        <th class='columnName'>Titre</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td class='columnUser'></td>
        <td class='columnDate'></td>
        <td class='columnPatient'>

        </td>
        <td class='columnName'>
          <strong></strong>
        </td>
      </tr>
    </tbody>
  </table>
</body>